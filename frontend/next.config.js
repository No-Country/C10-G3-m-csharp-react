/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["wachin93-001-site1.ftempurl.com", "inclusive.somee.com"],
  },
};

module.exports = nextConfig;
